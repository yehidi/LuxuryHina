# מדריך לפאנל הניהול של LuxuryHina

מדריך פשוט, שלב אחר שלב — גם אם אתם לא "טכניים" בכלל. קחו את הזמן בפעם
הראשונה; אחרי זה זה עניין של דקה.

## מה זה בעצם

פאנל הניהול הוא עמוד נפרד באתר שלכם, בכתובת:

```
https://yehidi.github.io/LuxuryHina/admin.html
```

ממנו אתם יכולים לשנות כל טקסט, תמונה, סרטון וגם **את כל סגנון העיצוב** של
האתר החי — בלי לגעת בקוד, ובלי לבקש מאף אחד. השינויים לא עולים לאוויר עד
שאתם לוחצים במפורש על "פרסום שינויים".

---

## שלב 1: יצירת טוקן גישה (פעם אחת בלבד)

הטוקן הוא כמו "מפתח" שנותן לפאנל הניהול הרשאה לעדכן רק את האתר הזה. יוצרים
אותו פעם אחת ושומרים אותו — לא צריך ליצור טוקן חדש בכל כניסה.

1. היכנסו לכתובת (מומלץ להעתיק-הדביק בדפדפן):
   `https://github.com/settings/personal-access-tokens/new`
   *(אם תתבקשו — התחברו לחשבון ה-GitHub שלכם קודם.)*

2. תחת **Token name** תנו שם, למשל `LuxuryHina Admin`.

3. תחת **Repository access** בחרו **Only select repositories**, ואז
   בחרו את **LuxuryHina** מהרשימה. ⚠️ זה השלב הכי חשוב — הוא זה שמבטיח
   שהטוקן יכול לגעת **רק** באתר הזה, ולא בשום דבר אחר.

4. תחת **Permissions**, פתחו את **Repository permissions**, מצאו את השורה
   **Contents**, ושנו אותה מ-`No access` ל-**`Read and write`**.

5. גללו למטה ולחצו על **Generate token**.

6. GitHub יראה לכם טוקן שמתחיל ב-`github_pat_...`. **העתיקו אותו עכשיו** —
   זו ההזדמנות היחידה לראות אותו במלואו.

> **בטיחות:** הטוקן נשמר אך ורק בדפדפן שלכם, במחשב הזה. הוא אף פעם לא
> נשלח לשום מקום חוץ מ-GitHub עצמו. אם יום אחד תרצו לבטל אותו — חוזרים
> לאותו עמוד ולוחצים **Delete** ליד השם שנתתם לו.

---

## שלב 2: כניסה לפאנל

1. גשו ל-`https://yehidi.github.io/LuxuryHina/admin.html`
2. הדביקו את הטוקן שהעתקתם בשלב הקודם
3. לחצו **התחבר**

מהכניסה הבאה ואילך הטוקן כבר שמור בדפדפן, והכניסה תהיה אוטומטית.

---

## שלב 3: עריכה

בצד ימין רואים תפריט עם כל אזורי האתר. לוחצים על אזור, עורכים את השדות,
והשינוי נשמר אוטומטית **בטיוטה** — עדיין לא באתר החי.

### עיצוב ותבנית (הכי כיף)
בראש התפריט. שלוש כרטיסיות עיצוב — Candlelit (זהב על שחור, העיצוב
הנוכחי), Light (בהיר ונקי) ו-Bold (אזמרגד עמוק). לוחצים על אחת, והיא
מסומנת כנבחרת. אפשר להחליף כמה פעמים שרוצים לפני הפרסום.

### הוספה/הסרה/סידור מחדש
בסקציות שירותים, גלריה, המלצות ושאלות נפוצות יש כפתור **"+ הוספה"**
בתחתית הרשימה, ובכל פריט — חיצי **↑ ↓** לשינוי סדר וכפתור פח-אשפה
למחיקה (יבקש אישור לפני מחיקה בפועל).

### העלאת תמונות וסרטונים
בכל מקום שיש שדה מדיה (Hero, אודות, כל פריט בגלריה) יש כפתור **"העלאת
תמונה"** או **"העלאת סרטון"**. בוחרים קובץ מהמחשב או מהטלפון — זהו, אין
צורך להעלות לשום מקום אחר קודם.

---

## שלב 4: תצוגה מקדימה

לפני שמפרסמים, אפשר ללחוץ **"תצוגה מקדימה"** למעלה — זה פותח את האתר
האמיתי בטאב חדש, **עם כל השינויים שעוד לא פורסמו**, בדיוק כפי שהם ייראו
ללקוחות. סוגרים את הטאב וממשיכים לערוך כרגיל.

---

## שלב 5: פרסום

כשמרוצים מהשינויים — לוחצים **"פרסום שינויים"** למעלה. ייפתח חלון קטן
שמראה בדיוק מה מתעדכן (כל תמונה בנפרד, קובץ התוכן, ולפעמים גם תגיות
השיתוף לגוגל/וואטסאפ). תוך כמה שניות האתר החי מתעדכן.

הנקודה למעלה ליד "כל השינויים פורסמו" משתנה לצבע זהב כשיש טיוטה שלא
פורסמה, כדי שלא תשכחו.

---

## שאלות נפוצות

**"הטוקן שלי לא עובד"**
ודאו שבחרתם בשלב 3 של יצירת הטוקן בדיוק את המאגר `LuxuryHina`, ושבשלב 4
נתתם הרשאת `Read and write` תחת Contents — לא רק קריאה.

**"אני רואה שגיאה על טוקן שפג תוקף"**
טוקנים יכולים לקבל תאריך תפוגה ביצירה. חוזרים לשלב 1 ויוצרים טוקן חדש
(אפשר גם בלי תאריך תפוגה, אם נוח לכם יותר).

**"פרסמתי משהו בטעות"**
כל שינוי היסטורי שמור ב-GitHub (בקובץ `content.json` ובתמונות שהעליתם),
אז שום דבר לא הולך לאיבוד באמת. אם צריך לחזור אחורה — כתבו לנו.

**"האתר לא התעדכן אחרי הפרסום"**
לפעמים לוקח לדפדפן כמה שניות/רענון להראות את הגרסה החדשה. נסו לרענן
(Ctrl+R או Cmd+R) בעמוד האתר החי.

**"אני רוצה לערוך ממספר מכשירים"**
אין בעיה — פשוט צרו את אותו טוקן (או טוקן חדש) ותתחברו איתו גם מהמכשיר
השני, דרך אותו עמוד `admin.html`.
